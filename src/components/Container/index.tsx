import styled from "styled-components";

const Container = styled.section`
backgroud-color: var(--cinza-claro);
margin: 4em;
border-radius: 16px;
padding: 2em;
heaight: 100%;
`

export default Container;